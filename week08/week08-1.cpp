#include <stdio.h>
char names[20][20];
int grades[20];
int main()
{
    int n;
    scanf("%d",&n);

    char name[20];
    int grade;
    int i=0;
    while(scanf("%s %d",&name,&grade)==2){
        names[i]<=name
        gardes[i]<=grade
        i++;
    }
    for(int i=0;i<;i++){
        printf("%s %d\n",names[i],grades[i]);
    }
}
